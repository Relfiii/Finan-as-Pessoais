import 'package:flutter/material.dart';

class CardSaldo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Detalhes do Saldo')),
      body: Center(child: Text('Aqui vão os detalhes do saldo')),
    );
  }
}